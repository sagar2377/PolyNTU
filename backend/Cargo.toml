[package]
name = "polyntu"
version = "0.2.0"
edition = "2024"
rust-version = "1.88"
description = "PolyNTU outcome markets: deterministic pricing and transactional simulated trading"

[dependencies]
axum = { version = "0.8", features = ["json"] }
tokio = { version = "1", features = ["full"] }
sqlx = { version = "0.9", default-features = false, features = ["runtime-tokio", "tls-rustls-ring", "postgres", "json", "migrate", "macros"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
rust_decimal = { version = "1", features = ["maths"] }
uuid = { version = "1", features = ["v4"] }
rand = { version = "0.10", features = ["sys_rng"] }
hmac = "0.13"
sha2 = "0.11"
base64 = "0.23"
argon2 = "0.6"
thiserror = "2"
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
tower-http = { version = "0.7", features = ["cors", "trace", "limit", "fs"] }
async-stream = "0.3"
futures-core = "0.3"

[dev-dependencies]
tower = { version = "0.5", features = ["util"] }
http-body-util = "0.1"
proptest = "1"

[profile.release]
overflow-checks = true
lto = "thin"

[[bench]]
name = "engine"
harness = false
