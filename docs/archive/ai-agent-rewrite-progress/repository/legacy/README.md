Archived PolyNTU options prototype.

`python-backend/` preserves the pre-Rust Python implementation and tests for historical reference. It is not part of the active backend, build, API or test suite. The old SQLite database remains in its original local location and has a separate local backup. See `docs/migration.md` for the cutover and data policy.
